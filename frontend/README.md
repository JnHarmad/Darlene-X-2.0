# DEPRECATED — use `web/` instead

This `frontend/` tree is a prototype for **Darlene-X_2.0**.

**Canonical offline engine:** [`../web/`](../web/README.md)

```bash
cd ../web
python -m http.server 8080
# open http://127.0.0.1:8080
```
